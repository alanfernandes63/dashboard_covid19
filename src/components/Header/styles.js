import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    header:{
        height:70,
    },
    paper:{
        paddingLeft:5,
        marginTop:10
    },
    containerIconMode:{
        paddingBottom:8,
        paddingTop:8
    },
    title:{
        fontSize:21,
        fontWeight:'bold'
    },
    iconTheme:{
        cursor:'pointer'
    }
});
export default useStyles;