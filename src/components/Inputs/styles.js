import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    container:{
        marginTop:8,
        marginBottom:7,
    },
    p:{
        margin:0
    }
});

export default useStyles;